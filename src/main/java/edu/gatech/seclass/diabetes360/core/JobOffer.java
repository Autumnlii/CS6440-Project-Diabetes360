package edu.gatech.seclass.diabetes360.core;

public class JobOffer extends Job {
    public JobOffer(String title, String company, Location location, float yearlySalary, float yearlyBonus, float restrictedStockAward, float relocStipend, Integer holidays, float score) {
        super(title, company, location, yearlySalary, yearlyBonus, restrictedStockAward, relocStipend, holidays, score);
    }
}
