package edu.gatech.seclass.diabetes360.core;

public class CurrentJob extends Job {
    public CurrentJob(String title, String company, Location location, float yearlySalary, float yearlyBonus, float restrictedStockAward, float relocStipend, Integer holidays, float score) {
        super(title, company, location, yearlySalary, yearlyBonus, restrictedStockAward, relocStipend, holidays, score);
    }
}
